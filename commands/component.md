# Create React Component

Create a new React component named **$ARGUMENTS**.

## Requirements

- Use **TypeScript**
- Implement a **functional component** using React hooks
- Define a clear **props interface** for the component
- Apply **basic styling using Tailwind CSS**
- Create a **test file alongside the component**
- Follow the existing **project component patterns** found in:
  - `src/components/`

## Expectations

- The component should be clean, readable, and reusable
- Match the folder structure and naming conventions used in the project
- Keep the implementation simple unless otherwise required
- Include reasonable default props if applicable

## Output

- Generate the component file
- Generate the corresponding test file
- Ensure both files align with current project standards
